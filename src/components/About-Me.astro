---
import aboutImage from '../assets/portrait.png';
---

<section class="about-section">
    <div class="about-content">
        <h2 class="section-title">About Me</h2>
        <div class="bio-container">
            <div class="bio-text">
                <h2 class="intro">
                    Hi, it's great to meet you!
                </h2>
                <p class="details">
                    {(() => {
                        const birth = new Date('2003-04-29');
                        const today = new Date();
                        let age = today.getFullYear() - birth.getFullYear();
                        const m = today.getMonth() - birth.getMonth();
                        if (m < 0 || (m === 0 && today.getDate() < birth.getDate())) age--;
                        return `I'm Vivian Klein, a ${age} year old computer science student at University of Applied Sciences Dortmund.`;
                    })()}
                    With experiences in both computer science related jobs, as well as customer-focused roles, I bring a wide range of
                    skills and interpersonal experience to every project I undertake. 
                    I'm always keen to work on new challenges, both professionally and personally :)
                </p>
                <h3 class="focus-areas">
                    Key information:
                </h3>
                <ul class="skills-list">
                    <li class="skill-item">Prior Experience in Customer Service</li>
                    <li class="skill-item">Managed Web Development Projects</li>
                    <li class="skill-item">Professional Insights in Web Design</li>
                    <li class="skill-item">Years of team-based project experience</li>
                </ul>
            </div>
            <div class="image-card">
                <img src={aboutImage.src} alt="About image" class="about-image" />
            </div>
            <div class="full-width-section">
                <h2 class="intro">
                    Experience & Skills
                </h2>
                <!-- Use a div here instead of a p to avoid placing a block <ul> inside a paragraph -->
                <div class="details">
                    <ul>
                        <li>Abitur in 2021 from Goethe-Schule Bochum, with a focus on Maths and English</li>
                        <li>Currently pursuing a Bachelor's degree in Computer Science with expected graduation in 2027</li>
                        <li>Internships at McCANN Worldgroup in Düsseldorf and at cv cryptovision GmbH in Gelsenkirchen</li>
                        <li>Part-time job experience in customer service, web development, and web design at DWDigi Essen</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
    .about-section {
        padding: 4rem 2rem;
        margin-top: 2rem;
    }

    .section-title {
        font-family: var(--font-heading);
        font-size: 2.5rem;
        color: var(--primary-color);
        margin-bottom: 2rem;
        text-shadow: var(--glow-subtle);
        position: relative;
    }

    .section-title::after {
        content: '';
        position: absolute;
        bottom: -0.5rem;
        left: 0;
        width: 60px;
        height: 2px;
        background: linear-gradient(
            90deg,
            rgba(var(--glow-accent), 0.5) 0%,
            rgba(var(--glow-accent), 0) 100%
        );
    }

    .bio-container {
        display: grid;
        grid-template-columns: 1.5fr 1fr;
        gap: 3rem;
        /* stretch so the image column matches the bio text height */
        align-items: stretch;
    }

    .bio-text {
        background: rgba(var(--glow-accent), 0.08);
        backdrop-filter: blur(12px);
        border-radius: 1.5rem;
        padding: 2rem;
        box-shadow:
            0 8px 32px rgba(0, 0, 0, 0.1),
            inset 0 0 0 1px var(--glow-soft);
    }

    .intro {
        font-size: 1.25rem;
        margin-bottom: 1.5rem;
        font-weight: 500;
    }

    .details {
        color: var(--text-color);
        line-height: 1.6;
        margin-bottom: 1.5rem;
    }

    .focus-areas {
        font-weight: 500;
        margin-bottom: 0.75rem;
    }

    .skills-list {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
        gap: 1rem;
        list-style: none;
        padding: 0;
        margin: 0;
    }

    .skill-item {
        color: var(--text-color);
        background: color-mix(in srgb, var(--secondary-color) 10%, transparent);
        padding: 0.5rem 1rem;
        border-radius: 1rem;
        border: 1px solid color-mix(in srgb, var(--secondary-color) 40%, transparent);
        text-align: center;
        transition: all 0.3s ease;
        display: flex;
        align-items: center;
        justify-content: center;
        min-height: 2.5rem;
    }

    .skill-item:hover {
        background: color-mix(in srgb, var(--secondary-color) 15%, transparent);
        transform: translateY(-2px);
        box-shadow: var(--glow-subtle);
    }

    /* image column that fills the remaining space and matches bio height */
    .image-card {
        height: 100%;
        display: block;
        border-radius: 1.25rem;
        overflow: hidden;
        border: var(--button-outline);
    }

    .about-image {
        display: block;
        width: 100%;
        height: 100%;
        object-fit: cover;
    }

    .full-width-section {
        grid-column: 1 / -1;
        background: rgba(var(--glow-accent), 0.08);
        backdrop-filter: blur(12px);
        border-radius: 1.5rem;
        padding: 2rem;
        box-shadow:
            0 8px 32px rgba(0, 0, 0, 0.1),
            inset 0 0 0 1px var(--glow-soft);
    }

    .full-width-section ul {
        list-style: none;
        padding: 0;
        margin: 0;
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 1.5rem;
    }

    .full-width-section li {
        background: rgba(var(--glow-accent), 0.05);
        border-radius: 1rem;
        padding: 1rem 1.5rem;
        line-height: 1.6;
        border: 1px solid color-mix(in srgb, var(--primary-color) 20%, transparent);
        transition: all 0.3s ease;
        position: relative;
        overflow: hidden;
    }

    .full-width-section li::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        width: 8px;
        height: 100%;
        background: var(--primary-color);
        box-shadow: var(--glow-subtle);
    }

    .full-width-section li:hover {
        transform: translateY(-2px);
        background: rgba(var(--glow-accent), 0.08);
        box-shadow: var(--glow-subtle);
        border-color: color-mix(in srgb, var(--primary-color) 30%, transparent);
    }

    @media (max-width: 768px) {
        .bio-container {
            grid-template-columns: 1fr;
        }


        .about-section {
            padding: 3rem 1rem;
        }

        .section-title {
            font-size: 2rem;
        }
    }
</style>
