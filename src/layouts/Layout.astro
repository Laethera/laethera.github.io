<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="preconnect" href="https://fonts.googleapis.com" />
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
		<link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet" />
		<link href="https://fonts.googleapis.com/css2?family=Noticia+Text:ital,wght@0,400;0,700;1,400;1,700&display=swap" rel="stylesheet" />
		<meta name="generator" content={Astro.generator} />
		<title>Vivian Klein</title>
	</head>
	<body>
		<slot />
	</body>
</html>

<style is:global>
	:root {
		--header-height: 4rem;
		--font-heading: 'Noticia Text', serif;
		
		--glow-accent: 214, 144, 159;

		--glow-color: rgba(var(--glow-accent), 1);
		--glow-light: rgba(var(--glow-accent), 0.45);
		--glow-soft: rgba(var(--glow-accent), 0.18);

		--glow-primary: 0 0 20px var(--glow-color);
		--glow-secondary: 0 0 36px var(--glow-light);
		--glow-subtle: 0 0 14px var(--glow-soft);

		--button-outline: 1px solid rgba(var(--glow-accent), 0.4);
		--focus-outline: 2px solid rgba(var(--glow-accent), 0.8);

		/* Variables */
		--background-color-1: #d8e1ee;
		--background-color-2: #dfc1c5;

		--primary-color: #764248;
		--secondary-color: #375781;

		--text-color: #001514;
	}

	html {
		font-family: 'Inter', system-ui, -apple-system, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, 'Noto Sans', sans-serif;
		background: linear-gradient(90deg, var(--background-color-1), var(--background-color-2));
		color: var(--text-color);
	}

	body {
		margin: 0;
		width: 100%;
		min-height: 100vh;
	}

	main {
		margin-top: 2rem;
		padding: 2rem;
	}

	h1, h2, h3, h4, h5, h6, p {
		margin: 0;
	}

	h1 {
		font-family: var(--font-heading);
		font-weight: 700;
		letter-spacing: 0.05em;
	}

	h2, h3 {
		color: var(--secondary-color);
	}
</style>
